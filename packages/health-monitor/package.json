{
  "name": "@ottochain/health-monitor",
  "version": "0.1.0",
  "description": "Metagraph health monitoring service — fork detection + automated restart",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "tsx --test test/*.test.ts",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "ssh2": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.33",
    "@types/ssh2": "^1.11.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
